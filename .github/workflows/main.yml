name: My First Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print Message
        run: echo "Hello from GitHub Actions Pipeline 🚀"
